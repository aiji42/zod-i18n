tasks:
  dev:
    command: 'tsup'
    args:
      - '--watch'
    inputs:
      - 'src/**/*'
      - 'tsup.config.ts'
    options:
      runInCI: false
  build:
    command: 'tsup'
    inputs:
      - 'src/**/*'
      - 'tsup.config.ts'
    outputs:
      - 'dist'
